# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Vercel CLI 在 CI 拉下來的專案設定與建置產物（vercel pull / vercel build）
.vercel

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
src/data/intlTaxOps/.sync-manifest.json

# 斷點續作機制的 transient session 狀態（見 ~/.claude/rules/斷點續作.md）
# 2026-07-18 起按專案拆成 CHECKPOINT.<slug>.md；星號一併蓋掉舊單檔與所有 per-project 檔
.claude/CHECKPOINT*.md
# 工程歷史（2026-07-20 起）。本 repo 是公開的，歷史檔含未發表研究數字、PhD 申請策略與絕對路徑，
# 一律不上 origin。該目錄自帶一個只在本機的 git repo 提供版本還原，勿在此加白名單。
.claude/history/

# JIRS 資料倉 PDF 的本機開發符號連結（正式站用 VITE_JIRS_PDF_BASE，PDF 不進 canvas 倉）
public/jirs

# 誤放在 canvas 根目錄的東西。本 repo 是公開的，而 `git add -A` 只要跑過一次就進歷史、
# 拿不回來，所以先擋住再說。目前擋的是 data/materials/職權分類獨立覆核-A.json——雙人
# 獨立覆核的編碼者原票，未發表，而且它是盲編碼，另一位編碼者看到就作廢。它的家在
# ../constitutional-court-research-data，該搬回去（搬是那條線的事，這裡只負責別讓它外流）。
/data/
# 臨時截圖／探查腳本（Playwright 一次性用完即丟，不該進版控）
_shot*.mjs

# vercel link 會自己在這裡加一行 `.env*`——那會連 .env.production 一起蓋掉，而那個檔
# 是公開值、必須進版控（VITE_SITE_URL 是 canonical／sitemap 的單一來源）。只擋本機的。
.env.local
.env*.local

# 《手記》的內容不進這個公開 repo（2026-07-29 使用者裁定：站上公開沒關係，但不要留在
# GitHub）。文章住在私有的 mt019/notes-data，由 .github/workflows/deploy.yml 在建置當下
# clone 下來、跑它的 sync 產生下面這些檔，建置完就隨 runner 一起消失。本機開發時它們
# 一樣由 `npm run sync` 產生，只是不進版控。
#
# 已經發表過的那四篇（fertility-care-responsibility、foreign-languages-and-translation、
# iias-colophon、karl-kraus）仍留在 git 歷史裡——那是已經公開的文章，不必改寫歷史。
# 這裡擋的是「從今以後不再有新的進來」。
src/data/notes.json
src/data/notes.sync.json
src/data/notes-archive.json
src/content/notes/
src/content/notes-archive.mdx
public/notes-assets/
