# Codepoints known to be absent from every bundled face for their role, verified
# 2026-07-06 with fontkit against the source font pack (Translation/_Assets/fonts).
# They render via the CSS fallback stack (Songti TC / Noto Serif CJK TC / serif).
# One codepoint per line; '#' starts a comment.
U+1D62  # ᵢ subscript i (AirPollutionFee formula) — not in Huiwen Mincho, Chiron Sung HK, or GenWanMin2; Latin-safe via system serif
